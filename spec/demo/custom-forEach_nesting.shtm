foreach:
<c:forEach items="${list}" var="item">
    <c:forEach items="${item.list}" var="item2">
        ${item2}
    </c:forEach>
</c:forEach>
<c:forEach items="${maps}" var="item">
    ${item.value}
</c:forEach>